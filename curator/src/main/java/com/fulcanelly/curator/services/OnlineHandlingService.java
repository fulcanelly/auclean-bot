package com.fulcanelly.curator.services;

public class OnlineHandlingService {
    void createOnlieLog(String reportedByUserId, String onlineOfUserId, String onlineStatus) {

        // TODO create online log realted to reported by user (session)
        // but create telegramuser for onlineOfUserId if does not exists yet
    }
}
